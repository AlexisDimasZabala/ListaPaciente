<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.alexdim.listapacientes">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ListaPacientes">
        <activity android:name=".ActivarPaciente"></activity>
        <activity android:name=".Editar_historial_clinico" />
        <activity android:name=".nuevo_doctor" />
        <activity android:name=".nuevo_paciente_medicos" />
        <activity android:name=".nuevo_paciente" />
        <activity android:name=".Menu" />
        <activity android:name=".HistorialClinico" />
        <activity android:name=".VistaPaciente" />
        <activity
            android:name=".Paciente"
            tools:ignore="Instantiatable" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>